name: idle_core
description: Deterministic idle engine core with tick simulation and offline progress.
version: 0.1.0

environment:
  sdk: ">=2.18.0 <4.0.0"

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
