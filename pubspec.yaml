name: idle_core
description: Core runtime for idle game development with deterministic ticks, offline progress, and replay.
version: 0.2.0
repository: https://github.com/tac0de/idle_core

environment:
  sdk: ">=2.18.0 <4.0.0"

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
