name: idle_core
description: Core idle game engine SDK for Dart with deterministic ticks, offline progress, replay, and versioned saves.
version: 0.3.1
repository: https://github.com/tac0de/idle_core

environment:
  sdk: ">=2.18.0 <4.0.0"

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
